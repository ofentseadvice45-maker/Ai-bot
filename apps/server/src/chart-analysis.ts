import type { ChartAnalysisRequest, ChartAnalysisResult } from '@manyama/shared';
export interface ChartAnalysisService { analyze(input: ChartAnalysisRequest): Promise<ChartAnalysisResult>; }
export class SafeChartAnalysisService implements ChartAnalysisService { async analyze(input: ChartAnalysisRequest): Promise<ChartAnalysisResult> { const scannedAt = new Date().toISOString(); if (!input.imageBase64.startsWith('data:image/')) return { status: 'UNCLEAR', valid: false, reasons: ['A valid image upload is required'], scannedAt }; if (!input.symbol || !input.timeframe) return { status: 'UNCLEAR', valid: false, reasons: ['Symbol and timeframe are required'], scannedAt }; return { status: 'UNCLEAR', valid: false, symbol: input.symbol, timeframe: input.timeframe, reasons: ['No vision model is configured; no setup was fabricated'], invalidation: 'Do not trade from an unclear image', scannedAt }; } }
