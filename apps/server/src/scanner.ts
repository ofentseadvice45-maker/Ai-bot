import type { CandleFeatures, ScannerSetup } from '@manyama/shared';

export const MIN_CONFIDENCE = 75;
export function score(features: CandleFeatures): number { return Math.min(100, Math.round((features.h4Bias !== 'NEUTRAL' ? 20 : 0) + (features.h1Structure ? 15 : 0) + (features.bos ? 15 : 0) + (features.supplyDemand ? 15 : 0) + (features.liquidity ? 15 : 0) + (features.momentum ? 10 : 0) + (features.rr >= 2 ? 10 : 0))); }
export function confidenceGate(confidence: number): boolean { return Number.isFinite(confidence) && confidence >= MIN_CONFIDENCE; }
export function scan(symbol: string, entry: number, stopLoss: number, takeProfit1: number, takeProfit2: number, features: CandleFeatures): ScannerSetup { const confidence = score(features); const direction = features.h4Bias === 'BULLISH' ? 'LONG' : features.h4Bias === 'BEARISH' ? 'SHORT' : 'NEUTRAL'; const status = confidenceGate(confidence) && direction !== 'NEUTRAL' ? 'VALID_SETUP' : confidence >= 55 ? 'WATCHING' : 'NO_SETUP'; return { symbol, direction, entry, stopLoss, takeProfit1, takeProfit2, rr: features.rr, confidence, bias: features.h4Bias, bos: features.bos, liquidity: features.liquidity, supplyDemand: features.supplyDemand, confirmation: features.momentum, reasons: ['H4 bias', 'H1 structure', 'M15 confirmation'].filter((_, index) => [features.h4Bias !== 'NEUTRAL', features.h1Structure, features.momentum][index]), invalidation: `Invalid if price crosses ${stopLoss}`, status }; }
